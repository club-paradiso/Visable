# Figma 확정안 → 라이브 반영 계획 (2026-07)

**상태:** Figma 13개 페이지 전체 확정 완료 (2026-07-24)
**Figma 파일:** `pInhK8Oyg04lpL4PMSCB4l`
**원칙:** Figma-first. 라이브는 화면별로 **개별 PR**로 선택 반영. 한 번에 대규모 재작성 금지.

---

## 0. 이미 반영된 것

| 항목 | PR | 상태 |
|---|---|---|
| 라이브 UI de-slop (헤더·마퀴·게이트웨이 장식 이모지 제거, i18n 14개 팩 포함) | #544 | ✅ 머지 완료 |
| 법률 근거 계층 + 통합 AI 검색 + 취업정보 신고 분석 전면 개선 | #545 | ✅ 머지 완료 |

> **참고 — #545와 UX Workspace의 관계**
> #545가 통합 AI 검색·법률 근거 계층·취업정보 신고 분석의 **기능 구현**을 이미 들여왔다.
> 즉 아래 "신규 기능 트랙"의 선행 조건(백엔드)이 상당 부분 충족된 상태다.
> 다음 작업 시 **먼저 `#545`가 실제로 구현한 범위를 읽고**, `UX-10 Component Contract`의
> 컴포넌트·상태와 대조해 **차이 나는 부분만** UI에 반영한다. 중복 구현을 만들지 않는다.
>
> **대조는 완료됐다.** 결과는 `docs/design/FIGMA_CODE_RECONCILIATION_2026_07.md` 에 있다.
> #545가 함께 들여온 `UNIFIED_SEARCH_AND_LEGAL_RESEARCH_CONTRACT.md` 와 Figma UX Workspace가
> 같은 것을 다른 이름으로 부르고 있었고, 계약에만 있고 시안에는 없던 상태(`no_evidence`,
> `LinkState=PlainText`, 미인식 코드, 근거 배지 4척도 분리)를 Figma에 채웠다.

---

## 1. 반영 우선순위

우선순위는 **(사용자 가치 × 구현 안전성) ÷ 회귀 위험**으로 정했다.
`index.html`은 단일 파일 대규모 앱이므로, UX Lock §2 보존 계약을 넘지 않는 범위에서만 움직인다.

### P1 — 낮은 위험 · 즉시 가치 (먼저)

| # | 화면 | Figma | 대상 파일 | 작업 성격 | 위험 |
|---|---|---|---|---|---|
| 1 | 공통 상태 세트 (로딩·빈결과·에러) — ✅ **반영 완료** | 91 | `index.html` CSS + 기존 상태 마크업 | 스타일·카피 정비 | 낮음 |
| 2 | 서류 체크리스트 — ⛔ **P3로 이동** | 14 | `index.html` 체크리스트 영역 | 아래 사유 참조 | ~~낮음~~ 높음 |
| 3 | Light/Dark 대비 교정 — ✅ **반영 완료** | 13 | `index.html` 토큰 | 색·대비만 조정 | 낮음 |

**공통 지침:** 마크업 구조·`id`·`data-action`은 그대로 두고 CSS와 문구만 바꾼다.

> **P1-1 반영 기록 (완료).** 대상은 대행기관·지정의료기관 파인더의 로딩·빈결과·에러다.
> 메인 검색의 빈 결과는 보존 계약이 걸린 메인 `<script>` 안에서 생성되므로 **이번 범위에서 제외**했다.
> 그건 마크업이 아니라 스크립트를 건드려야 하는 일이라 P3에서 별도로 다룬다.
>
> **작업 중 발견한 함정 — 반드시 읽을 것.**
> `--color-surface` / `--color-text` / `--color-border` / `--color-error` 계열은
> **`[data-theme="dark"]` 에서 재정의되지 않는다.** `:root` 와 `archive_diary` 테마에만 정의돼 있다.
> 이 계열로 카드 배경을 칠하면 다크 모드에서 밝은 카드가 그대로 튀어나온다.
> 테마를 따르는 것은 `--bg0/--bg1/--bd/--t1/--t2/--ac/--cy` 계열뿐이다.
> **표면·보더·텍스트·오류색은 반드시 후자를 쓴다.** (§4 표의 매핑도 이 계열 기준이다.)

> **P1-2 는 P1 에서 착수 불가 — 실측으로 확인했다.** 두 겹으로 막힌다.
> 첫째, 체크리스트 타일은 보존 계약이 걸린 메인 `<script>` 안(`tagsHtml`)에서 생성된다.
> 진행률·저장/공유·완료 상태 UI 를 붙이려면 그 블록을 고쳐야 한다.
> 둘째, Figma 14 가 타일마다 보여주는 **발급처·조건부 표시가 현재 데이터에 존재하지 않는다.**
> 그걸 만들어 넣는 건 `CLAUDE.md` 가 금지한 "요건 신설"에 해당한다.
> 따라서 P1-2 는 **P3(스크립트 변경 트랙)** 으로 옮기고, 데이터 확장은 별도 안건으로 다룬다.

> **P1-3 반영 기록 (완료).** 계획서는 "결과 카드 CSS" 라고 적었지만 실제 원인은 더 아래였다.
> 랜딩 전반이 쓰는 **`--p-*` 팔레트가 `:root` 와 `archive_diary` 테마에만 정의돼 있고
> 기본 테마의 `[data-theme="dark"]` 에는 아예 없었다.** 그래서 다크 모드에서 섹션 제목·본문·
> 매뉴얼 카드가 어두운 잉크색 그대로 남아 어두운 배경 위에서 읽히지 않았다.
> 규칙을 하나씩 덮어쓰지 않고 **`--p-*` 다크 값만 정의**해 한 번에 해결했다.
> 이로써 다크에서 재정의되지 않는 토큰 계열은 `--color-*` 와 `--p-*` **둘 다**였음이 확인됐다.
> 새 컴포넌트를 만들 때 이 두 계열로 표면·텍스트를 칠하지 않는다.
>
> 검증: 같은 파일 2회 캡처(대조군) 라이트 변화 20.29% vs 변경 후 20.30% —
> 차이가 전부 애니메이션 캡처 노이즈이며 라이트 회귀가 없음을 확인했다.

### P2 — 중간 위험 · 큰 가치

| # | 화면 | Figma | 대상 파일 | 작업 성격 | 위험 |
|---|---|---|---|---|---|
| 4 | New Home 랜딩 Dark 대비 복구 — ✅ **반영 완료** | 32 | `new-home.html` | 다크 토큰 교정 | 중간 |
| 5 | New Home 준비상태 체크 모달 — ⚠️ **반영 금지 · 시안을 라이브 기준으로 갱신 완료** | 33 | (코드 변경 없음) | 아래 사유 참조 | — |
| 6 | Waymaker 워크스페이스 정비 | 21 | `ai.html` | 다크 대비·행 스타일 | 중간 |
| 7 | 서류 작성 도우미 — ✅ **대조 완료 · 구조 일치 · 시안 카피만 갱신** | 15 | (코드 변경 없음) | 아래 사유 참조 | — |
| 8 | 귀화면접 허브 | 34 | `nationality-interview-hub.html` | 섹션 카드 그리드 | 중간 |

> **P2-5 는 반영하지 않는다 — 라이브가 시안보다 앞서 있다.**
> 실제 모달을 열어 8문항을 끝까지 통과시켜 결과 화면까지 확인한 결과다.
>
> Figma 33 의 결과 화면은 세 묶음(준비됨 / 확인 필요 / 지금은 미해당)짜리 단순 요약이다.
> 라이브는 이미 **검토 가능 경로 · 준비하면 좋은 자료 · 공식 확인 필요 · 주의할 리스크**
> 네 축으로 나뉘어 있고, 경로마다 근거 문장과 접기/펼치기를 갖추고 있다.
> 공식 출처 안내도 시안보다 구체적이다 — 시안은 "관할 출입국·1345", 라이브는
> **"관할기관의 심사와 공식 기준(국적법령·법무부·하이코리아) 확인이 필요합니다"** 로
> 법령·주무부처·포털을 모두 짚는다. 면책도 결과 상단·중간·하단 세 곳에 걸려 있다.
>
> 즉 이 화면은 **시안을 라이브에 반영하면 기능이 후퇴한다.** 반영 대상에서 뺐다.
>
> **대신 Figma 33 을 라이브 기준으로 갱신했다 (완료).** 코드 변경은 없다.
>
> | 시안 (이전) | 시안 (갱신 후 · 라이브 기준) |
> |---|---|
> | ① 인트로 · ② 질문 · ③ 결과 | ① **결정 기록 카드** · ② 질문 · ③ 결과 |
> | 결과 = 세 묶음 요약 | 결과 = **네 축** + 경로별 근거·상태 필·접기 표시 |
> | 헤더 "준비 상태 요약" | 헤더 **"귀화 준비 점검"** |
> | "관할 출입국·1345" | **"관할기관의 심사와 공식 기준(국적법령·법무부·하이코리아)"** |
> | 질문 선택지 4개 | 선택지 **3개** (라이브 Q1 과 일치) |
> | CTA "정착 경로 찾기 →" | **"처음부터" / "귀화 경로 찾기"** 2버튼 |
>
> 인트로 상태(약 2분 / 로그인 없이 / 참고용 칩 + 시작하기)는 질문 앞에 관문을 하나 더
> 두는 것이라 라이브 쪽이 낫다고 판단해 시안에서 뺐다. 대신 그 자리에 **결정 기록 카드**를
> 두어 "왜 만들지 않는가"를 캔버스에 남겼다. 닫기 경로는 헤더 ✕ 로 항상 열려 있어
> 시안의 "항상 닫기 제공" 조건을 이미 만족한다.
>
> **시안에 일부러 남겨 둔 개선 후보 1건:** 질문 상태 푸터의 "닫기" 버튼은 라이브에 없다
> (✕ 만 있음). 그러나 §3 카피 원칙이 "다이얼로그 왼쪽 버튼은 닫기"라고 정하고 있어
> 시안 ② 에 남기고 반영 후보로 표시했다.

> **P2-4 New Home 랜딩 Dark — 반영 완료.** Light 을 기준으로 Dark 를 전면 대조했다.
> 8개 구간을 나란히 렌더해 비교한 결과, 구조·문구·카드 배치는 이미 완전히 일치했다.
> 실제 결함은 하나였다 — 다크 accent(`--ac` = `#3BE4B8`, 밝은 민트)에 흰 텍스트를
> 하드코딩해 쓴 곳 3군데(`.nh-btn-primary`, 여정 단계 번호 배지, `.nh-btn-sm.primary`)가
> **1.62:1** 로 사실상 안 보였다. 라이트는 같은 구조가 진한 초록(`#0E8A6B`)이라
> 흰 텍스트로 4.32:1 을 냈으므로 티가 안 났다.
>
> `--ac-ink` 토큰을 신설해 라이트는 흰색, 다크는 진한 잉크(`#083830`, 8.02:1)로
> 갈랐다. 세 규칙 모두 `color: #fff` → `var(--ac-ink)` 로 교체 — 색을 규칙마다
> 하드코딩하지 않고 토큰 값만 바꾸는 원칙을 그대로 따랐다.
> `archive_diary` 다크(`--ac` = `#6EC6FF`)에도 별도 처리 없이 상속으로 적용되며
> 6.91:1 을 낸다 — CSS 커스텀 프로퍼티는 요소에 직접 선언된 값이 조상 규칙의
> 명시도와 무관하게 이기므로, `[data-theme="dark"]` 가 `body` 에 직접 선언한
> `--ac-ink` 가 `:root[data-theme="archive_diary"]` 의 상속값보다 항상 우선한다.
>
> 검증: `check_repo.sh` 통과, 라이트·다크 전체 페이지 재캡처로 버튼·배지 판독 확인.

> **Figma page 32 (New Home 랜딩 Dark) — 별도로 재구축 완료 (2026-08).**
> 위 항목은 라이브 코드(`new-home.html`) 수정이었다. 사용자가 요청한 건 그게 아니라
> **Figma 작업창의 page 32 자체를 page 31(Light, 확정)을 기준으로 고치는 것**이었다.
>
> 확인해 보니 page 32 는 부분적 색상 버그가 아니라 **완전히 다른, 더 오래된 초안**이었다.
> 프레임 크기부터 달랐고(1280×1686 vs 확정 Light 의 1440×2330), 기능 카드가 8개 중 4개만
> 있었고, 정착 여정 스테퍼 표현도 달랐고, 페이지명에 다른 확정 페이지들과 달리 `✅ 확정`
> 표시도 없었다. 그리고 라이브에서 고친 것과 같은 흰-텍스트-온-민트 버그가 카드 제목
> 3곳에도 있었다.
>
> 부분 패치로는 구조 자체가 달라 신뢰할 수 없다고 판단해, **Light 프레임(121:5)을
> 그대로 복제해 page 32 로 옮기고, `new-home.html` 의 라이트→다크 토큰 값(`--bg0-3`,
> `--t1-3`, `--ac`, `--ac2`, `--acL`, `--cy`, `--cyL`, `--cyT`)을 그대로 이식해
> 재색칠**했다. 이러면 구조·카피가 Light 와 100% 동일함이 보장되고, 색도 이미 라이브에서
> 검증된 토큰이라 새로 판단할 게 없다. 기존 다크 초안은 지우지 않고 `x=-3000` 으로
> 옮기고 잠근 뒤 이름에 "OLD 초안"이라고 남겼다.
>
> 기계적 리매핑 중 발견해 고친 것 3건:
> 1. 카드 제목·섹션 헤딩 9곳이 CTA 버튼용 `--ac-ink` 매핑에 잘못 걸려 다크 잉크로
>    뒤집혀 안 보였다 — CTA 라벨 2개만 남기고 되돌렸다.
> 2. 히어로 배경이 `GRADIENT_LINEAR`(단색 아님)라 리매핑 스크립트가 건너뛰어, 라이트
>    크림색 그라디언트가 그대로 남아 히어로 전체가 다크처럼 안 보였다 — `--bg1→--bg2`
>    다크로 교정.
> 3. 장식용 bloom 타원(히어로 배경 흐림 효과)을 라이트 원색 그대로 옮기면 진한 배경
>    위에서 밝은 파스텔 얼룩으로 떴다 — `--cyL`/`--acL` 다크 워시 톤으로 교체.
>
> 확신도 배지의 체크마크(`--ok` 토큰, 라이브에도 다크 오버라이드가 없는 값)만 리매핑
> 부작용으로 흰색→다크잉크가 됐는데, 재계산해 보니 원래(백색 3.13:1)보다 오히려
> 대비가 살짝 나아져(3.86:1) 그대로 두었다.
>
> 검증: 전 노드 색상 인벤토리 재스캔으로 미매핑 색상 0건, 흰색 fill 0건, 그라디언트
> 1건(히어로, 확인 완료) 확인. 8개 카드·스테퍼·공식출처·푸터·경고 배너를 개별 확대
> 캡처로 육안 검수.

> **P2-7 서류 작성 도우미 — 대조 완료. UI 구조는 이미 일치한다.**
> 실제 페이지의 `.fh-step-panel` 을 전수로 뽑아 시안의 7단계 레일과 맞춰 봤다.
>
> | 시안 레일 | 라이브 |
> |---|---|
> | 서류 선택 | `어떤 서류를 작성하시나요?` |
> | 신청·신고 유형 | `신청/신고 유형 선택` |
> | 체류자격·목적 | `체류자격 및 목적` |
> | 기본 인적사항 | `기본 인적사항` |
> | 절차별 추가정보 | `절차별 추가정보` |
> | 누락·확인 항목 | `누락 및 확인 필요 항목` |
> | 작성 가이드 | `작성 가이드` |
>
> 7단계가 1:1 로 대응하고, 시안의 정본 미리보기 패널도 라이브에 `f06-preview-canvas` 로
> 이미 있다. **UI 정비 작업은 필요 없다.**
>
> 대신 **시안 카피가 라이브보다 뒤처져 있어 시안 쪽을 갱신했다 (완료, 코드 변경 없음).**
>
> - 제목 `순서대로 채우면 서류 준비 끝!` → `필수서류 작성 도우미`.
>   마케팅 톤이라 라이브의 담백한 제목으로 맞췄다.
> - 설명문을 라이브 문장(정본 레이아웃 위에 배치된 PDF 로 내려받는다 + 제출 전 최신
>   공식 서식·관할 관서 확인)으로 교체.
> - **`개인정보 유출 걱정 없이 간편하게!` → 사실 고지로 교체.** 이게 핵심이다.
>   시안 문구는 **안전을 단정**하는데, 라이브는 검증 가능한 사실만 말한다 —
>   "브라우저 메모리 안에서만 사용 · 서버 전송/저장 없음 · localStorage 미사용 ·
>   PDF 는 브라우저 안에서 빈 정본 위에 직접 작성". 개인정보 카피는 단정하지 않는다.
>   필 하나였던 것을 2줄 고지 카드로 바꾸고 우측 열을 재배치했다.
>
> **시안에 아직 반영하지 않은 라이브 기능 2건** (화면을 더 그려야 해서 남겨 둠):
> 서류 3종 분기(통합신청서/국내거소신고서 · 거주/숙소제공확인서 · 신원보증서)와
> 한국어·영어 병기. 둘 다 라이브가 앞선 항목이므로 **라이브를 건드리지 않는다.**
>
> **교훈:** 이 계획서는 "Figma 확정 = 목표"를 전제했지만, 화면에 따라 라이브가 더 나아간
> 경우가 있다. **포팅 전에 실제 화면을 끝까지 돌려보고 어느 쪽이 앞서는지 먼저 판정한다.**

### P3 — 높은 위험 · 신중히 (마지막)

| # | 화면 | Figma | 대상 파일 | 이유 |
|---|---|---|---|---|
| 9 | Visable 랜딩 풀페이지 | 11 | `index.html` | 히어로 구조 변경 — 검색 진입점에 직접 영향 |
| 10 | 검색→결과 흐름 | 12 | `index.html` | 핵심 사용자 여정. A/B 검증 후 반영 권장 |
| 11 | 반응형 브레이크포인트 | 92 | `index.html` | 전역 CSS 영향 범위가 큼 |
| 12 | 테마 시스템 토큰 정렬 | 90 | 전 파일 | 토큰 변경은 모든 화면에 파급 |

### 별도 트랙 — 신규 기능 (UX Workspace)

`UX-02` ~ `UX-10`의 통합검색 · 리걸 리서치 · 취업신고는 **기존 화면 개선이 아니라 신규 기능**이다.
`UX-10 Component Contract`의 컴포넌트↔코드↔API 매핑을 사양으로 삼아 별도 개발 트랙으로 진행한다.

백엔드 엔드포인트는 **#545에서 이미 구현됐다.** 실제 라우트는 다음과 같다
(`backend/paradiso_backend.py` 기준 — 이전 판에 적혀 있던 `/api/unified-search`,
`/api/ai-overview`는 존재하지 않는 경로였다):

- `POST /api/search/unified` — 결정론적 오가닉 결과. AI·외부 HTTP 없음
- `POST /api/search/unified/ai-overview` — `ok` · `unavailable` · `no_evidence` · `not_applicable`
- `POST /api/employment/interpret` — 검증된 사실만. KSCO8/KSIC11 코드는 절대 포함하지 않는다

---

## 2. PR 단위 규칙

1. **화면 1개 = PR 1개.** 여러 화면을 한 PR에 묶지 않는다.
2. PR 본문에 대응 Figma 페이지 번호를 명시한다.
3. 변경 전후 스크린샷을 첨부한다.
4. **보존 계약(UX Lock §2) 점검을 PR마다 수행한다:**
   - `id` / `data-action` / 인라인 `onclick` 유지
   - `<body class="landing" data-theme="light">` 유지
   - `window.PARADISO_BACKEND_URL` config 스크립트 유지
   - 메인 `<script>` 블록 무변경
5. i18n 문구를 바꾸면 **14개 언어 팩 전부** 반영하고 JSON 유효성을 검증한다.
6. 리걸/이민 관련 문구·면책·출처 경고는 축소·삭제하지 않는다.

---

## 3. 카피 원칙 (앱인토스 UX 라이팅 기반)

- **해요체**로 통일한다.
- 능동형·긍정형으로 쓴다. ("없어요" → "~하면 할 수 있어요")
- 다이얼로그 왼쪽 버튼은 **"닫기"**. "취소"는 쓰지 않는다.
- 과도한 경어("~시겠어요?", "~께")를 쓰지 않는다.
- CTA는 누른 뒤 무엇이 일어나는지 알 수 있게 쓴다.
- **"추천" ≠ "가능"** — 자격 판단을 단정하지 않는다.
- AI 산출물은 "참고용"임을 항상 병기하고, 공식 검색 결과보다 권위 있게 보이지 않게 한다.

---

## 4. 디자인 토큰 (라이브 적용 기준)

| 토큰 | 라이트 | 다크 | 용도 |
|---|---|---|---|
| 배경 | `#F7F4EF` | `#08271F` | 페이지 |
| 카드 | `#FFFCF5` + 1px `#E6E6EE` | `#0D3129` + white 8% | 카드 표면 |
| 본문 | `#1C1F29` | `#EEF4F1` | 텍스트 |
| 보조 | `#4D5261` | `#A8BDB5` | 캡션 |
| 주 CTA | `#0B4F44` + 흰 텍스트 (AAA) | `#3BE4B8` + `#083830` | 버튼 |
| 강조 | `#177366` | `#3BE4B8` | 링크·포커스 |
| 경고 | `#F2C879` | 동일 | 확인 필요 |
| 오류 | `#D95C47` | 동일 | 에러 |

**주의:** 라이트 카드(`#FFFCF5`)는 배경과 명도가 가까워 **1px 보더가 필수**다.
다크의 "흰색 8%" 보더는 라이트에서 `#E6E6EE` 실선으로 대체한다.

**더 중요한 주의:** 위 표는 **목표값이지 현황이 아니다.** 현재 `index.html`의 실제 값은 다르다
(예: 라이트 보더 `--bd`가 지금은 회색이 아니라 갈색 `#998058`이다).
Figma 토큰 ↔ CSS 변수 ↔ 현재 라이브 값의 대조표는
`docs/design/FIGMA_CODE_RECONCILIATION_2026_07.md` §4.3 에 있다.
색을 옮길 때는 규칙마다 하드코딩하지 말고 **변수 값을 바꾼다.**

---

## 4-A. 오로라 + 사이트 전체 가독성 패스 (2026-08)

> **New Home 다크 히어로에 오로라 도입 + 5개 페이지 가독성 교정.**

### 오로라 — 색을 새로 만들지 않았다

다크 New Home 히어로가 `var(--vp-paper)` 단색이라 완전히 평평했다.
색을 새로 고르는 대신 **이미 확정돼 있던 하우스 레시피**를 찾아서 이식했다 —
Figma page 29 `20 Waymaker ✅ 히어로` 의 **"Waymaker Hero · Dark Aurora"(`198:5`)**.

레시피의 핵심은 **"어두운 wash 톤이 아니라 밝은 accent 를 아주 낮은 알파로"** 다.
어두운 톤을 쓰면 다시 평평해진다. (재구축 당시 이 함정에 한 번 빠졌었다.)

| 레이어 | 값 |
| --- | --- |
| base | `#0E3028 → #051A15` (세로) |
| bloom-mint | `#3BE4B8` @ .15 |
| bloom-peri | `#5A63A8` @ .24 |
| bloom-ember | `#FF8B7A` @ .09 |
| bloom-glow ×2 | `#F3EEDF` @ .07 / .05 |
| 전부 | `LAYER_BLUR 190` |

- **라이브**(`assets/css/visable-product-2026.css`): 위 5겹을 `radial-gradient` 로 옮겼다.
  베이스는 아래에서 `--vp-paper` 로 착지시켜 본문과의 이음매를 없앴다.
  시안의 State A/B/C Smart-Animate 루프에 대응하는 26s 표류는 `::before` 로 분리했다
  (그래디언트 재도색 없이 transform 만). `prefers-reduced-motion` 에서는 정지한다.
- **알파 조정:** 라이브만 mint `.15→.13`, glow `.07→.06`. 시안과 달리 실제 히어로에는
  본문이 얹히는데, 원본 값이면 민트 블룸 정점에서 보조 텍스트가 4.46:1 로 기준
  미달이다. 조정 후 4.91:1. **시안(page 32)은 그 지점에 본문이 없어 스펙값 그대로 둔다.**

### 가독성 — 추정하지 않고 실제 픽셀을 measured

CSS 에서 배경색을 추정하면 그래디언트·반투명·겹침에서 틀린다. 대신 각 페이지를
두 번 렌더링했다 — 한 번은 정상, 한 번은 `-webkit-text-fill-color: transparent` 로
**글자만** 지우고(테두리·배경·currentColor 파생 페인트는 그대로). 두 번째 렌더가
진짜 배경이고, 거기서 텍스트 위치의 픽셀을 뽑아 대비를 계산했다.

5개 페이지 × 라이트/다크 = **2,536개 텍스트 런**. **425건 미달 → 0건.**

| 원인 | 전 | 후 |
| --- | --- | --- |
| 3차(muted) 잉크 — 실패의 대부분 | 라이트 3.10:1 / 다크 2.53:1 | 4.6:1↑ |
| 다크 블록이 `--t1/--t2` 로 연결 안 됨 | 페이지 인라인 토큰이 그대로 | 라이트와 대칭 |
| 밝은 민트 위 흰 글자 (버튼·칩 라벨) | 1.62:1 | 6.9~8.0:1 |
| `ai.html` 라이트 `--p-emerald-soft` | 1.87:1 | 5.29:1 |
| `ai.html` 라이트 `.char-counter` | **1.00:1** (안 보임) | 7.4:1 |
| 브랜드 브릿지 히어로 부제 | 2.67:1 | 4.66:1 |
| "공식 사이트 확인" 주의 배지 | 2.58:1 | 4.69:1 |

**주의 문구는 약화하지 않았다.** 경고 배지는 문구를 그대로 두고 더 잘 보이게만 조정했다.

**측정 하네스 자체의 오탐 3건을 걸러냈다** — 스크롤 리빌로 `opacity:0` 인 요소,
`background-clip:text` 그래디언트 글자, 그리고 Chromium 이 일부 색을
`color(srgb …)`(0–1)로 보고하는데 `rgb()`(0–255)로 파싱한 것. 마지막 건은 크림색을
검정으로 읽어 실패를 크게 부풀렸다(`ai.html` 라이트가 35건 → 실제 10건).
**하네스가 "고장났다"고 말하기 전에 하네스부터 의심할 것.**

### 시안 동기화

라이브 muted 잉크를 바꿨으니 시안도 같이 옮겨야 한다. 그런데 확인해 보니 시안은
`#6E7A74`/`#7A8580` 로, 프로덕션 스킨(`--vp-ink-3` `#74817C`)과 **이미 달랐다** —
스킨이 나중에 들어오면서 생긴 기존 격차다. 시안 값도 3.05~4.36:1 로 자체 기준 미달이었다.

12개 제품 페이지의 **TEXT fill 114개**를 라이브와 같은 값으로 옮겼다.
단순 치환이 아니라 **각 노드의 가장 가까운 칠해진 조상의 휘도를 재서** 라이트
배경이면 `#5A6561`, 다크 배경이면 `#BBC7C3` 를 고르게 했다 — 같은 hex 가 다크
표면에서 쓰이는 경우 반대로 뒤집히는 것을 막는다. (page 32 가 실제로 13개 모두
다크 쪽으로 매핑됐다.)

- 제외: page 32 의 `x=-3000` 잠금 보존된 옛 초안.
- `32 New Home · 랜딩 Dark` 에 `✅ 확정` 표시를 붙였다. 확정 Light 에서 1:1 파생됐고
  확정된 하우스 오로라를 쓰므로 더 이상 미확정으로 둘 이유가 없다.
  README 페이지 맵(`218:9`)은 이미 `32 랜딩 Dark ✅` 로 적혀 있어 표기 불일치도 해소됐다.

### 스펙 보드 90/91/92 (2026-08)

**91·92 는 순수 목업이었다** — hex 문서화도 스와치도 없어서 다른 페이지와 같은
방식으로 캡션 색만 옮겼다(각각 4건 / 21건).

**90 (테마 시스템)은 달랐다. 보드 전체가 낡은 팔레트를 문서화하고 있었다.**
프로덕션 스킨(`visable-product-2026.css`, 2026-07)이 라이트 팔레트를 바꿨는데
스펙 보드에 반영되지 않은 것이다. 추정하지 않고 **실제 사이트에서
`getComputedStyle` 로 토큰을 측정해** 그 값으로 맞췄다.

| 토큰 | 보드(전) | 실측(후) |
| --- | --- | --- |
| bg0 L | `#F4EFE4` | `#F4F0E7` |
| surface L | `#FFFCF5` | `#FFFEFA` |
| bg2 L | `#ECE5D6` | `#FBFAF6` |
| t1 L / D | `#073B32` / `#F3EEDF` | `#0B211B` / `#F1EEE5` |
| t2 L / D | `#3A544C` / `#C7BFA8` | `#41564F` / `#C1CCC6` |
| bd L | `#D1C6B4` | `#D3CAB8` |
| accent L / D | `#0E8A6B` / `#34D4A8` | `#0B7A61` / `#3BE4B8` |
| Primary CTA L / D | `#085E48` / `#085E48` | `#0B7A61` / `#34D4A8` |
| warn D | `#F2C879` | `#E68A3A` (코드에 다크 오버라이드 없음) |

- **`t3`(보조 텍스트 2) 행이 아예 없었다.** 이번에 바꾼 토큰인데 문서화가 빠져
  있어 행을 추가했다 — L `#5A6561` / D `#BBC7C3`.
- **검증:** 22개 셀 전부 스와치 fill 과 인쇄된 hex 라벨이 일치하고, 모두 실측값과
  같다. 낡은 muted 잉크 잔존 0건.

> **⚠️ 판단이 필요한 건 — Primary CTA 가 §5 기준(AAA 7:1)에 미달한다.**
> 보드는 "라이트/다크 공통 deep emerald(`#085E48`) + 양 모드 AAA" 라고 적고
> 있었지만, 실측하면 **라이트 `#0B7A61` + 흰 텍스트 = 5.29:1**,
> **다크 `#34D4A8` + 잉크 `#083830` = 6.88:1** 이다. 둘 다 AA 는 넘지만 AAA 는
> 아니다. 다크가 밝은 민트로 바뀌면서 "공통 deep emerald" 전제가 깨진 결과다.
>
> 스펙 보드는 **사실대로 `(AA)` 로 정정**해 뒀다 — 틀린 접근성 주장을 보드에
> 남겨 두는 쪽이 더 위험하기 때문이다. 다만 **CTA 색을 진하게 바꿔 AAA 를
> 맞출지, §5 기준을 AA 로 조정할지는 디자인 결정**이라 임의로 정하지 않았다.

---

## 5. 접근성 기준 (PR 머지 전 확인)

- 본문 대비 WCAG AA 4.5:1 이상, 주 CTA AAA 7:1
- 최소 터치 영역 44×44px
- 포커스 링 2px + offset 2 (`focus-visible`)
- 색만으로 정보를 전달하지 않는다 (상태는 색 + 텍스트 병기)
- 390px에서 가로 스크롤이 생기지 않는다

---

## 6. 다음 액션

1. **P1-1 (공통 상태 세트)** 부터 착수 — 위험이 가장 낮고 전 화면에 파급 효과가 있다.
2. 각 PR은 Vercel 프리뷰로 시각 확인 후 머지한다.
3. P3는 P1·P2 반영 후 안정성을 확인하고 착수한다.
